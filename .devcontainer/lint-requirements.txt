# Static analysis for this project's own shell/Python setup scripts (not the C/NLM code under
# test - see AGENTS.md). Versions pinned for reproducibility, matching this repo's general
# pin-everything convention.
ruff==0.16.0
pylint==4.0.6
pip-audit==2.10.1
# pylint (unlike ruff) does real import resolution, so it needs qmp.py's own runtime dependency
# importable here too, even though it's a separate concern from lint-venv's own tools - see
# qemu/requirements.txt for the pin this mirrors.
qemu.qmp==0.0.6
